# AngularNgxsForReduxdevtoolsNoStoreFoundDemo

This repository was created to demonstrate https://github.com/zalmoxisus/redux-devtools-extension/issues/772

For run application clone this repository and run:

`npm i`

then run:

`npm run start`
